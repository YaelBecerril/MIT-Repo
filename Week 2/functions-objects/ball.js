var length = data.length;

// you work goes here
// -----------------------
// Variables para el código

var dataCounter = -1

//Función que creará las bolitas



//Loop para crear bolitas

while (dataCounter <= 1)
{
    dataCounter++;
    data[dataCounter];
    let x = data[dataCounter].x;
    let y = data[dataCounter].y;
    let color = data[dataCounter].color;
    var ball = data [400];
    create (ball.x, ball.y, ball.color);
   console.log(create);

}